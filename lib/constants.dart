import 'package:flutter/material.dart';

class Constants {
  static const appBarHeight = Size.fromHeight(140);
  static const appBarPadding = EdgeInsets.only(
    left: 30,
    bottom: 20,
    right: 30,
  );
  static const appBarColor = Color(0xfff52c56);

  static const cardHeight = 250.0;

  static const borderRadius = 10.0;

  static const placeIconSize = 50.0;

  static const bodyPadding = 20.0;

  static const socialIconSize = 20.0;
}
