import 'package:flutter/material.dart';

class Constants {
  static const appBarHeight = Size.fromHeight(100);
  static const appBarPadding = EdgeInsets.symmetric(
    horizontal: 30,
    vertical: 10,
  );
  static const appBarColor = Colors.pinkAccent;

  static const cardHeight = 250.0;
}
